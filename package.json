{
  "name": "ecommerce-vibe-pulse",
  "private": true,
  "version": "1.0.0",
  "description": "VibePulse — Ecommerce Sprint 2: Página Principal y Catálogo",
  "scripts": {
    "dev": "concurrently \"pnpm run dev:client\" \"pnpm run dev:server\"",
    "dev:client": "cd client && pnpm run dev",
    "dev:server": "cd server && pnpm run dev",
    "install:all": "pnpm install && cd client && pnpm install && cd ../server && pnpm install",
    "build": "cd client && pnpm run build && cd ../server && pnpm run build",
    "prisma:migrate": "cd server && npx prisma migrate dev",
    "prisma:seed": "cd server && pnpm run prisma:seed",
    "prisma:studio": "cd server && npx prisma studio"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
